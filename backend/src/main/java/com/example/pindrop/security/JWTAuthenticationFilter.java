package com.example.pindrop.security;

public class JWTAuthenticationFilter {
    // This class will handle JWT authentication logic
    // It will intercept requests and validate JWT tokens
    // If the token is valid, it will set the authentication in the security context
    // If the token is invalid, it will return an unauthorized response
}
